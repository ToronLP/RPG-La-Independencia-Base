/// @description Insert description here
// You can write your code in this editor



switch(my_id)
{
	case 0:
		if(room != rm_options)room_goto(rm_options)
	break;
	
	case 1:
		if(room != rm_sound)room_goto(rm_sound)
	break;
	
	case 2:
		if(room != rm_options)room_goto(rm_controls)
	break;
}