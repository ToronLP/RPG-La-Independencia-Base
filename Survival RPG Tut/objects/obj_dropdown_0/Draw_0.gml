/// @description Insert description here
// You can write your code in this editor
draw_self();

if(obj_dropdown_controller.first_id == id)
{
	draw_text(x,y,string(obj_resolution_controller.windowWidth) + "x" + string(obj_resolution_controller.windowHeight))	
}

